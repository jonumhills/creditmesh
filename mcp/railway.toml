[build]
builder = "nixpacks"
buildCommand = "cd mcp && npm install"

[deploy]
startCommand = "cd mcp && npx tsx src/index.ts"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
